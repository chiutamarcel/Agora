<Window x:Class="Agora.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:Agora"
		xmlns:views="clr-namespace:Agora.MVVM.View"
		mc:Ignorable="d"
		Title="MainWindow" Height="1080" Width="1920">

	<!--<views:UserSettingsView></views:UserSettingsView>-->
	<Grid Background="{StaticResource PrimaryHueDarkBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition MinWidth="450" Width="2*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="60"/>
			<RowDefinition />
		</Grid.RowDefinitions>

		<!--Navbar-->
		<Grid Background="{StaticResource SecondaryHueMidBrush}" Grid.Row="0" Grid.ColumnSpan="3">

			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="60"/>
				<ColumnDefinition Width="*"/> <!--Spacer-->
				<ColumnDefinition Width="450"/>
				<ColumnDefinition Width="150"/>
				<ColumnDefinition Width="*"/> <!--Spacer-->
				<ColumnDefinition Width="auto"/>
			</Grid.ColumnDefinitions>

			<!--Logo Btn-->
			<Button Height="auto" Style="{StaticResource MaterialDesignIconButton}" Click="logoBtn_Click">
				<Image Grid.Column="0" Source="\MVVM\Images\AgoraLogo.png"/>
			</Button>

			<!--Search Bar-->
			<TextBox 
				Grid.Column="2" 
				Width="auto"
				Margin="0 5"
				HorizontalAlignment="Stretch" 
				VerticalAlignment="Stretch"
				Background="White"
				Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
			
			<Button
				Grid.Column="3"
				Height="50"
				Width="auto"
				Margin="5 0"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				Style="{StaticResource MaterialDesignPaperButton}">
				Search
			</Button>
			<!--End Search Bar-->
			
			<!--User Settings Button-->
			<Button
				Name="UserButton"
				Grid.Column="5"
				Height="50"
				FontSize="22"
				Style="{StaticResource MaterialDesignFlatDarkButton}"
				Click="userBtn_Click">
				<TextBlock Text="{Binding UserName}"/>
			</Button>

		</Grid>

		<!--MainContent-->
		<ScrollViewer 
			Grid.Column="1" 
			Grid.Row="1" 
			ScrollViewer.CanContentScroll="True" 
			VerticalScrollBarVisibility="Visible"
			HorizontalScrollBarVisibility="Disabled">
			<Frame 
				Name="MainContentFrame"  
				Source="{Binding CurrentPage}" 
				Background="{StaticResource MaterialDesignBackground}"
				NavigationUIVisibility="Hidden"/>
		</ScrollViewer>


	</Grid>

</Window>
