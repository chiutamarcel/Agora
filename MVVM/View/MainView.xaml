<Window x:Class="Agora.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:Agora"
		xmlns:views="clr-namespace:Agora.MVVM.View"
		mc:Ignorable="d"
		Title="MainWindow" Height="1080" Width="1920">

	<!--<views:UserSettingsView></views:UserSettingsView>-->
	<Grid Background="{StaticResource PrimaryHueDarkBrush}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition MinWidth="450"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="50"/>
			<RowDefinition />
		</Grid.RowDefinitions>

		<!--Navbar-->
		<Grid Background="{StaticResource SecondaryHueMidBrush}" Grid.Row="0" Grid.ColumnSpan="3">
			
		</Grid>

		<!--MainContent-->
		<ScrollViewer 
			Grid.Column="1" 
			Grid.Row="1" 
			ScrollViewer.CanContentScroll="True" 
			VerticalScrollBarVisibility="Visible"
			HorizontalScrollBarVisibility="Disabled">
			<Frame Name="MainContent"  Source="MainListView.xaml" Background="{StaticResource MaterialDesignBackground}"/>
		</ScrollViewer>


	</Grid>

</Window>
